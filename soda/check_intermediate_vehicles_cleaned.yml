checks for int_src_vehicles_cleaned:
  - duplicate_count(vehicle_id):
    warn: when > 0
    fail: when > 1    
  - missing_count(vehicle_id):
    warn: when > 0
    fail: when > 1
  - values in vehicle_id must exist in int_src_sales_condensed vehicle_id
  - invalid_count(transmission) = 0:
    valid values: ["AT", "MT"]
  - invalid_count(updated_at) = 0
    valid format: date inverse